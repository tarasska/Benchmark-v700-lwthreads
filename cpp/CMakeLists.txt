cmake_minimum_required(VERSION 3.16)
project(Benchmark-v700)

set(CMAKE_CXX_STANDARD 20)
set(BENCH_DIR "microbench")
set(DS_DIR "ds")
set(BUILD_DIR "bin/")
set(RECLAIM ".debra")

#set(SOURCE_EXE main.cpp)
#set(CMAKE_CXX_FLAGS "-lpthread -ldl -mrtm")
list(APPEND CMAKE_CXX_FLAGS_LIST "-lpthread -ldl -mrtm ")
list(APPEND CXX_LIBS_LIST "pthread" "dl")

set(CMAKE_EXPORT_COMPILE_COMMANDS ON CACHE INTERNAL "")
if(CMAKE_EXPORT_COMPILE_COMMANDS)
    set(CMAKE_CXX_STANDARD_INCLUDE_DIRECTORIES
      ${CMAKE_CXX_IMPLICIT_INCLUDE_DIRECTORIES})
endif()

### if libpapi is installed but you do not want to use it, invoke make with extra argument "has_libpapi=0"
execute_process(COMMAND "./${BENCH_DIR}/_check_lib.sh papi" OUTPUT_VARIABLE has_libpapi)
if (NOT ${has_libpapi} MATCHES "0")
    add_definitions("-DUSE_PAPI")
    list(APPEND CXX_LIBS_LIST "papi")
endif ()

### if libnuma is installed but you do not want to use it, invoke make with extra argument "has_libnuma=0"
execute_process(COMMAND "./${BENCH_DIR}/_check_lib.sh numa" OUTPUT_VARIABLE has_libnuma)
if (NOT ${has_libnuma} MATCHES "0")
    add_definitions("-DUSE_LIBNUMA")
    list(APPEND CXX_LIBS_LIST "numa")
endif ()

option(USE_ASAN "USE_ASAN" OFF)
if (USE_ASAN)
    add_definitions("-fsanitize=address" "-static-libasan")
endif ()

option(USE_ASSERTS "USE_ASSERTS" ON)
if (USE_ASSERTS)
    add_definitions("-DNDEBUG")
endif ()

option(USE_FOPENMP "USE_FOPENMP" ON)
if (USE_FOPENMP)
    find_package(OpenMP)
    list(APPEND CXX_LIBS_LIST "${OpenMP_CXX_FLAGS}")
endif ()

option(USE_TIMELINES "USE_TIMELINES" OFF)
if (USE_TIMELINES)
    add_definitions("-DMEASURE_TIMELINE_STATS")
endif ()

option(NO_OPTIMIZE "NO_OPTIMIZE" OFF)
if (NO_OPTIMIZE)
    add_definitions("-O0" "-fno-inline-functions" "-fno-inline")
else ()
    add_definitions("-O3")
endif ()

option(SKIP_VALIDATION "SKIP_VALIDATION" OFF)
if (SKIP_VALIDATION)
    add_definitions("-DSKIP_VALIDATION")
endif ()

# if (USE_STACK_OPERATIONS)
    # add_definitions("-DUSE_STACK_OPERATIONS")
# endif ()
# if (USE_QUEUE_OPERATIONS)
    # add_definitions("-DUSE_QUEUE_OPERATIONS")
# endif ()
set(QUEUE_DS
    treiber_stack_fc_sleep
    treiber_stack_fc_sleep_mcs
    treiber_stack
    treiber_stack_fast
    treiber_stack_fc
    stack_nasl_mcs
)

add_definitions("-DMAX_THREADS_POW2=2048"
        "-DMAX_COROUTINES_POW2=8192"
        "-DCPU_FREQ_GHZ=2.1"
        "-DMEMORY_STATS=if\(1\)"
        "-DMEMORY_STATS2=if\(0\)"
        "-std=c++20 -mcx16 -gdwarf"
        "-fno-omit-frame-pointer"
        "-DDEBRA_ORIGINAL_FREE"
        "-DKEY_DEPTH_TOTAL_STAT"
        "-DKEY_SEARCH_TOTAL_STAT"
        "-Wno-volatile"
        #"-DUSE_COROUTINES"
        "-DWITH_PARALLEL_WORK")

set(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS_LIST})
message(CMAKE_CXX_FLAGS=${CMAKE_CXX_FLAGS})

include_directories(.)
include_directories(common)
include_directories(common/atomic_ops)
include_directories(common/atomic_ops/atomic_ops)
include_directories(common/dcss)
include_directories(common/descriptors)
include_directories(common/kcas)
include_directories(common/papi)
include_directories(common/recordmgr)
include_directories(common/rlu)
include_directories(common/rq)
include_directories(common/rq/snapcollector)
include_directories(common/urcu)

include_directories(${BENCH_DIR})

find_package(Threads REQUIRED)

set(SOURCE_EXE ${BENCH_DIR}/main.cpp)

file(GLOB ADAPTERS ${DS_DIR}/*/adapter.h)
string(REGEX MATCHALL "\/[^\/]+\/adapter.h" RAW_DATA_STRUCTURES ${ADAPTERS})

foreach (DATA_STRUCTURE ${RAW_DATA_STRUCTURES})
    #    message(ADAPTER="${ADAPTER}")
    string(REPLACE "\/adapter.h" "" DS ${DATA_STRUCTURE})
    string(REPLACE "\/" "" DS ${DS})
    list(APPEND DATA_STRUCTURES ${DS})
endforeach ()

# Verlib
include_directories(ds/verlib_structs/include)

# NASL
# ── Boost ─────────────────────────────────────────────────────────────────────
if (Boost_ROOT)
    message(STATUS "Boost: using custom path: ${Boost_ROOT}")
    set(Boost_NO_SYSTEM_PATHS ON)
endif ()
find_package(Boost REQUIRED COMPONENTS fiber fiber_numa)
message(STATUS "Boost: found ${Boost_VERSION} at ${Boost_INCLUDE_DIRS}")

# ── NASL ──────────────────────────────────────────────────────────────────────
if (NASL_ROOT)
    message(STATUS "NASL: using local path: ${NASL_ROOT}")
    set(NASL_LOCKS_INCLUDE_DIR        "${NASL_ROOT}/locks/include")
    set(NASL_FIBERS_BENCH_INCLUDE_DIR "${NASL_ROOT}/benchmarks/nasl-fibers-bench/include")

    foreach (_nasl_dir ${NASL_LOCKS_INCLUDE_DIR} ${NASL_FIBERS_BENCH_INCLUDE_DIR})
        if (NOT EXISTS "${_nasl_dir}")
            message(FATAL_ERROR "NASL: directory not found: ${_nasl_dir}\n"
                                "Check that NASL_ROOT points at the root of nasl-tools.")
        endif ()
    endforeach ()
else ()
    message(STATUS "NASL: NASL_ROOT not set — fetching from GitHub")
    include(FetchContent)
    FetchContent_Declare(
        nasl_tools
        GIT_REPOSITORY https://github.com/ITMO-PTDC-Team/nasl-tools.git
        GIT_TAG        main
        GIT_SHALLOW    TRUE
    )
    FetchContent_MakeAvailable(nasl_tools)
    set(NASL_LOCKS_INCLUDE_DIR        "${nasl_tools_SOURCE_DIR}/locks/include")
    set(NASL_FIBERS_BENCH_INCLUDE_DIR "${nasl_tools_SOURCE_DIR}/benchmarks/nasl-fibers-bench/include")
endif ()
message(STATUS "NASL locks  : ${NASL_LOCKS_INCLUDE_DIR}")
message(STATUS "NASL fibers : ${NASL_FIBERS_BENCH_INCLUDE_DIR}")

add_subdirectory(${NASL_LOCKS_INCLUDE_DIR}/.. nasl)

foreach (DS ${QUEUE_DS})
    message(DS_DIR= "${DS_DIR}/${DS}")
    set(DS_RUN ${DS}${RECLAIM})

    add_executable(${DS_RUN} ${SOURCE_EXE} common/nasl_boost_fibers/yield.cpp)
    #target_include_directories(${DS_RUN} PUBLIC ${DS_DIR}/nasl_boost_fibers)

    # target_include_directories(${DS_RUN} PRIVATE ${Boost_INCLUDE_DIRS})
    # target_link_libraries(${DS_RUN} PRIVATE ${Boost_LIBRARIES})
    target_include_directories(${DS_RUN} PUBLIC ${DS_DIR}/${DS})
    target_link_libraries(${DS_RUN} PUBLIC ${CXX_LIBS_LIST})
    target_link_libraries(${DS_RUN} PUBLIC "-latomic")

    target_link_libraries(${DS_RUN} PUBLIC nasl Boost::fiber Boost::fiber_numa)
    target_compile_definitions(${DS_RUN} PRIVATE USE_STACK_OPERATIONS)
endforeach ()
